<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>

<script type="text/javascript">
	// ----- prompt -----
	// prompt() 함수 좌변에 변수 선언 시 데이터 입력 후 확인 버튼 클릭하면 해당 데이터가 저장됨
	// => 이때, 취소 버튼 클릭 시 변수에 null 값이 저장되고,
	//    아무것도 입력하지 않고 확인 버튼 클릭 시 빈문자열 널스트링("") 값이 저장됨
	// ---------------------------------------------------------------------
	
	// 연습문제1)
	// 사용자로부터 숫자 1개를 입력받아 변수 inputNum에 저장 후
	// 입력받은 숫자가 짝수 or 홀수 판별
	// => 만약, 짝수일 경우 "xxx: 짝수!", 아니면 "xxx: 홀수!" 출력
	// => 짝수란? 어떤 수를 2로 나눈 나머지가 0
	let inputNum = prompt("숫자 1개 입력");
// 	let inputNum = 10;
// 	console.log(inputNum);

	let msg = inputNum + ": ";
	if (inputNum % 2 == 0) {
// 		msg = msg + "짝수!";
		msg += "짝수!";
// 		msg = "짝수!";
	} else {
		msg += "홀수!";
	} 
	console.log(msg); // 10: 짝수!

	// -----------------------------------------
	// 연습문제2)
	// "종료하시겠습니까?" 메세지를 출력하면서 사용자로부터 확인/취소 버튼 입력받아
	// 변수 isExit에 저장 후 판별 작업 수행
	// => 만약, 확인 버튼 클릭했을 경우 "종료합니다!", 아니면 "취소합니다!" 출력
	// => 확인 버튼 클릭 시 true 값 저장되고, 취소버튼 클릭 시 false 값 저장됨 (boolean타입)
	let isExit = confirm("종료하시겠습니까?");
	console.log("선택 결과: " + isExit + ", 데이터타입: " + typeof(isExit));
	
	
	
</script>

</head>
<body>

</body>
</html>